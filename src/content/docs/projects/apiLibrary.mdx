---
title: "Api Library"
description: "API REST para la gestión de libros, autores y categorías."
slug: "apiLibrary"
---

# Proyecto: Library

API REST para la gestión de libros, autores y categorías.

> [Código fuente](https://github.com/)

## Tecnologías

- Node.js + Express.js + TypeScript
- MongoDB + Mongoose
- Swagger
- Jest
- Docker

---

## Endpoints Principales

### 📚 Libros

- **GET** `/api/books` → Obtener todos los libros
- **GET** `/api/books/{id}` → Obtener un libro por ID
- **POST** `/api/books` → Crear un nuevo libro
- **PUT** `/api/books/{id}` → Actualizar un libro
- **DELETE** `/api/books/{id}` → Eliminar un libro

```json
{
  "serie": 2341,
  "title": "Title book",
  "author": "Author Name",
  "category": "Category Name",
  "publicationDate": "2077-07-12",
  "pagesNumber": 210,
  "synopsis": "A lot of text"
}
```

### ✍️ Autores

- **GET** `/api/authors` → Obtener todos los autores
- **GET** `/api/authors/{id}` → Obtener un autor por ID
- **POST** `/api/authors` → Crear un nuevo autor
- **PUT** `/api/authors/{id}` → Actualizar un autor
- **DELETE** `/api/authors/{id}` → Eliminar un autor

### 🏷️ Categorías

- **GET** `/api/categories` → Obtener todas las categorías
- **GET** `/api/categories/{id}` → Obtener una categoría por ID
- **POST** `/api/categories` → Crear una nueva categoría
- **PUT** `/api/categories/{id}` → Actualizar una categoría
- **DELETE** `/api/categories/{id}` → Eliminar una categoría

---

## Swagger - Documentación

Para ver la documentación interactiva, iniciar la API y acceder a:

```
http://localhost:3000/api-docs
```

---

## Testing

Ejecutar pruebas unitarias con Jest:

```sh
npm run test
```

---
